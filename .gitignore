# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual envs
.venv/
venv/
ENV/
env/

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
.pyright/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local user data (output, profiles, alerts)
contract-reviews/
contract-drafts/
matters/
out/
*.local.md

# Internal / draft docs — not part of public release
docs/

# Per-user / local Claude Code config (contains absolute paths)
.mcp.json
.claude/

# Secrets — никогда не коммитим
.env
.env.local
.env.*.local

# Auto-generated by adapters/claude-code/build.py
adapters/claude-code/output/

# External research / vendor submodules — не коммитим, у пользователей клонируется отдельно
vendor/
# Anthropic vendor submodule — клонируется через `git submodule update --init`
# Если нужно — клонируй вручную: git clone --depth 1 https://github.com/anthropics/claude-for-legal vendor/claude-for-legal

# Optional Node tooling (если решим добавить docs site)
node_modules/

# Caches от MCP серверов
.cache/
